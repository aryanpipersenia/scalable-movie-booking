server:
  port: 8082

spring:
  application:
    name: booking-service
  data:
    redis:
      host: localhost
      port: 6379 # Matches your docker-compose.yml

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/